# Copy this file to .env.local and fill in real values.
# NEVER commit the actual .env or .env.local file to GitHub.

# Google Apps Script Web App URL for the coming-soon email signup sheet
NEXT_PUBLIC_SHEET_SCRIPT_URL=

# Supabase — safe to expose client-side (anon/publishable key only)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# --- Added later as we build further stages ---
# SUPABASE_SERVICE_ROLE_KEY= (server-side only, never NEXT_PUBLIC_)
# RESEND_API_KEY=
# REVOLUT_SECRET_KEY=
# REVOLUT_WEBHOOK_SECRET=
