export const metadata = {
  title: 'Become an Athlete',
  description: 'The BLXCKSHARK Athlete program — invite-only. Train hard, tag us, get noticed.',
}

export default function AthletesLayout({ children }) {
  return children
}
