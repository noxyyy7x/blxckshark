export const metadata = {
  title: 'About Us',
  description: 'The story behind BLXCKSHARK — built for those who refuse to blend in.',
}

export default function AboutLayout({ children }) {
  return children
}
